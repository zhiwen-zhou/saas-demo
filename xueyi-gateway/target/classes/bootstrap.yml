# Tomcat
server:
  port: 8080

# Spring
spring:
  # redis 配置
  redis:
    # 地址
    host: 81.71.139.12
    # 端口，默认为6379
    port: 26300
    # 密码
    password: xhjORvesRf3kna6o
    database: 90
    # 连接超时时间
    timeout: 10s
    jedis:
      pool:
        # 连接池中的最小空闲连接
        min-idle: 0
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池的最大数据库连接数
        max-active: 8
        # #连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1ms
  application:
    # 应用名称
    name: xueyi-gateway
  profiles:
    # 环境配置
    active: dev
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 192.168.6.130:8848
        namespace: f14023f1-463c-4fa2-9171-c3f19774cf73
      config:
        # 配置中心地址
        server-addr: 192.168.6.130:8848
        namespace: f14023f1-463c-4fa2-9171-c3f19774cf73
        # 配置文件格式
#        file-extension: yml
        # 共享配置
#        shared-configs:
#          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
#          - application-secret-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
        data-id: dev
        group: DEFAULT_GROUP
        timeout: 3000
    sentinel:
      # 取消控制台懒加载
      eager: true
      transport:
        # 控制台地址
        dashboard: 127.0.0.1:8718
      # nacos配置持久化
      datasource:
        ds1:
          nacos:
            server-addr: 192.168.6.130:8848
            dataId: sentinel-xueyi-gateway
            groupId: DEFAULT_GROUP
            data-type: json
            rule-type: flow

nacos:
  gateway:
    route:
      config:
        data-id: xueyi-gateway
        group: DEFAULT_GROUP
